name: gitpub
description: A dart repository that can provide dart code from git sources and tags as dart versions.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.2

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0

dev_dependencies:
  http: ^1.1.0
  lints: ^5.0.0
  test: ^1.24.0
